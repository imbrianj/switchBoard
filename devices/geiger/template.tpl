
      <section id="{{DEVICE_ID}}" class="{{DEVICE_TYPE}}{{DEVICE_SELECTED}}{{DEVICE_STATE}}">
        <h2>{{i18n_GEIGER}} <em>{{DEVICE_ACTIVE}}</em></h2>
        <div class="text-block">
          <time datetime="{{GEIGER_TIMESTAMP}}">{{GEIGER_TIME}}</time>
          <ul>
            {{GEIGER_DYNAMIC}}
          </ul>
        </div>
      </section>