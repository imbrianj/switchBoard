
      <div id="{{DEVICE_ID}}" class="device {{DEVICE_TYPE}}{{DEVICE_SELECTED}}{{DEVICE_STATE}}">
        <div class="text-block">
          <h5>{{WEATHER_CURRENT}}</h5>
          <ul>
            {{WEATHER_FORECAST}}
          </ul>
        </div>
      </div>