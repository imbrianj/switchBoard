
      <section id="{{DEVICE_ID}}" class="{{DEVICE_TYPE}}{{DEVICE_SELECTED}}{{DEVICE_STATE}}">
        <h1>Stocks</h1>
        <div class="text-block">
          <ul>
            {{STOCKS_DYNAMIC}}
          </ul>
        </div>
      </section>