
      <section id="{{DEVICE_ID}}" class="{{DEVICE_TYPE}}{{DEVICE_SELECTED}}{{DEVICE_STATE}}" data-string-loss="{{i18n_LOSS}}" data-string-neutral="{{i18n_NEUTRAL}}" data-string-gain="{{i18n_GAIN}}">
        <h1>{{i18n_STOCKS}}</h1>
        <div class="text-block">
          <ul>
            {{STOCKS_DYNAMIC}}
          </ul>
        </div>
      </section>